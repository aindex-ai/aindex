[tool.poetry]
name = "aindex"
version = "0.1.1"
description = "Reusable AI functions"
authors = ["Eduardo Reis"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
